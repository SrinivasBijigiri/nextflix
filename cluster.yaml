apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: my-eks-cluster
  region: us-east-1
  version: "1.28"
nodeGroups:
  - name: ng-1
    instanceType: t2.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
